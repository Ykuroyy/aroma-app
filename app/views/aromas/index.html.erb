<div class="container mt-4">
  <!-- Header -->
  <div class="text-center mb-5">
    <div class="d-flex align-items-center justify-content-center gap-3 mb-4">
      <i class="fas fa-leaf text-success" style="font-size: 2rem;"></i>
      <h1 class="botanical-header mb-0">アロマを選んで今日の運勢を占ってみよう</h1>
      <i class="fas fa-leaf text-success" style="font-size: 2rem;"></i>
    </div>
    <p class="botanical-subtitle">心に響く香りを選んで、あなたの今日を素敵に彩りましょう</p>
  </div>
  
  <!-- Aroma Grid -->
  <% if @aromas.any? %>
    <div class="row g-4 justify-content-center">
      <% @aromas.each do |aroma| %>
        <div class="col-lg-3 col-md-4 col-sm-6">
          <div class="card botanical-card h-100">
            <div class="card-body d-flex flex-column text-center p-4">
              <!-- Aroma Name -->
              <%= link_to result_aroma_path(aroma), class: "aroma-name-link" do %>
                <h3 class="aroma-name"><%= aroma.name %></h3>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="text-center py-5">
      <div class="bg-white/80 backdrop-blur-sm rounded-3xl p-5 max-w-md mx-auto shadow-lg">
        <i class="fas fa-leaf text-success mb-3" style="font-size: 3rem;"></i>
        <p class="text-success fs-5">アロマが登録されていません。</p>
      </div>
    </div>
  <% end %>
  
  <!-- Footer decoration -->
  <div class="footer-decoration">
    <i class="fas fa-leaf"></i>
    <span>自然の香りで心を豊かに</span>
    <i class="fas fa-leaf"></i>
  </div>
</div>
